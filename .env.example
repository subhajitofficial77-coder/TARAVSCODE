# Copy this file to .env.local and fill in real values.

# NEXT_PUBLIC_ variables are exposed to client-side bundle. Keep secrets out of these.
NEXT_PUBLIC_SUPABASE_URL="https://your-project.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="your_anon_key_here"
SUPABASE_SERVICE_ROLE_KEY="your_service_role_key_here"
SUPABASE_DB_URL="postgresql://user:password@host:port/database"

## Phase 5: AI Integration
# OpenRouter API Key (required for AI chat - primary provider)
# Get your key at: https://openrouter.ai/keys
# OpenRouter provides access to multiple models (Claude, GPT family, etc.)
OPENROUTER_API_KEY="your_openrouter_api_key_here"

# Google AI API Key (fallback when OpenRouter fails)
# Create an API key for Google Generative Language and set the endpoint below.
GOOGLE_AI_KEY="your_google_ai_key_here"
GOOGLE_AI_ENDPOINT="https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent"

SERP_API_KEY="your_serp_api_key_here"
WEATHER_API_KEY="your_weather_api_key_here"
WEATHER_API_ENDPOINT="https://api.weatherapi.com/v1"

NEXT_PUBLIC_APP_URL="http://localhost:3000"
TARA_LOCATION="City,Country"
TARA_TIMEZONE="Region/City"

## Security notes
# - Do NOT commit `.env.local` or any file with real credentials.
# - Rotate any exposed keys immediately (the repo had secrets previously).
# - SUPABASE_SERVICE_ROLE_KEY must always remain server-side and never in client-exposed variables.

